
#include <stdio.h>

main ()
{
    int a,b,c;
    printf("enter first number a=");
    scanf("%d", &a);
    printf("enter second number b=");
    scanf("%d",&b);
    printf("previous swapping a=%d and b=%d\n " , a,b );
     c=a;
     a=c;
     b=c;
     printf("after swapping a=%d and b=%d", a,b);
     
    
    
    
    
}
